% Formation Control

% Based on the estimated leader positions on each individual follower, target positions for the followers are computed for each follower. 

The control strategy is orchestrated through a behavior tree framework, allowing for modular and adaptive control. The hybrid approach combines:

\begin{itemize}
    \item \textbf{Speed Control:} When cross-track deviation is low, focus on maintaining formation speed
    \item \textbf{Steering Control:} When cross-track deviation increases, prioritize lateral correction
    % \item \textbf{Turn Adaptation:} During leader turns, temporarily shift setpoint to maintain formation geometry
\end{itemize}